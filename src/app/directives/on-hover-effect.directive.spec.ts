import { OnHoverEffectDirective } from './on-hover-effect.directive';

describe('OnHoverEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new OnHoverEffectDirective();
    expect(directive).toBeTruthy();
  });
});
